# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs", "python3", "chromium"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npm run db:init && npm start"
